day1

dev by guido vaan rossum -1991
free and open source
portable

module types 
buitin 
externaal 

pip - package manager 

Rapl - cmd -> python   ---- read evaluate print load

comments - #
""" multi line comment """,''' ''' 

variables

data types 
integer 
float
string
boolean 
none

operators
arithmetic - +-/*
assignment - =,+=,-= etc
comparison - <,>, ==, <= ,!= etc 
logical - and,or,not 

type() function 
type(a)

type casting 

float(b) -- str to float
str(a)
int("wde")

input() function -- by default type is str 

a**2 - square of a 


strings

datatype 
sequence of chars 
immutable

'' "" ''' ''' - can define a str in three ways 

slice 
slice with skip value 
functions 
escape sequence characters
// written in code files

lists 
we can change lists , modify lists 
code files 

tuples are immutable 
written in code files 


sets and dictionaries

dictionaries 
- key value pairs 
- unordered
- mutable - we can modify 
- indexed 
- cant contain duplicate keys 


sets 
collection of well defined objs
-unordered 
-unique 
-unindexed 
-immutable 

// we cant add list as a element in sets 

conditional statements 
- relatrional operators 
- logical operators 


loops

while loops
for loops  

Inn for loop iterate item using in 
for , else - it happens only for loop breaks out  


break and continue 

pass - it does nothing simply passes it to nxt operation 

completed loops concept

started functions 

return a vaalue 
default parameter 
recursion

Started project - game
completed project 


file handling 

1) text files
2)binary files

open - open files 
read - read files 
close - closes the file 
file handling 
w - overwrites an existing file if not creates one 
f.readlines() - returns all line in a list 

practice problems cmpleted - filehandling 

oops 
class - bluprint for object 

object - instance of a class

attribute - belongs to class

instance attributes 
self parameter - refers to created object 
self used as parameter to create methods 
@staticmethod - no need to define a self in method as a parameter
we can use pass in classes to avoid error 

__init__() - constructor called when object created, dunder method  starts with double underscore 
takes self as arguments and many

The __str__() method is a special method that controls what is returned when the object is printed

to access class attribute -- class_name.attribute 

package import 

import random -- random.randint(1,5)
from random import randint -- randint(1,5)

inheritance 
types of inheritance 
single inheritance -- p1->c1
multiple inheritance p1,p2 -> c1
muilti level inheritance  -- p1->c1->c2

super() - parent class
super().__init__() - parnt class constructor 




need to revisit
--------------
@classmethod - bound to the class not the object of the class  
instead of self we use cls 

property decorators 
@property -- we can return any value 
e.function_name 

@func_name.setter 
e.func_name = 23

operator overloading 

__str__() -- method 

__len__()
















 
